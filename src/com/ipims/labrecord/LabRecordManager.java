package com.ipims.labrecord;

import com.ipims.database.DatabaseManager;
import com.ipims.models.LabRecord;
import com.ipims.models.Patient;

public class LabRecordManager {

	public void viewLabRecord(String userName) {
		
	//	DatabaseManager.getInstance().getLabRecord(userName);
		
	}
	
	public void EnterLabRecord(String userName) {
		
		
		 //  DatabaseManager.getInstance().EnterLabRecord(userName);
			
			
	}
	
	public void UpdateLabRecord(String userName) {
		
     
	 //  DatabaseManager.getInstance().DeleteLabRecord(userName);
		
		//Then...
		
	 //  DatabaseManager.getInstance().EnterLabRecord(userName);
		
		
	}
	
	
}
